#include <stdio.h>
#include <string.h>

/*Let me add a C file to the repo*/

int main(void)
{
    char myName[] = "David";
    printf("%s is a Software Engineer", myName);
    putchar('\n');
    return 0;
}